{# Die echte Config (per `sudo cat` vom Referenzsystem geholt) enthaelt
   auth-user-pass/script-security/up/down bereits selbst mit exakt den
   Pfaden, die diese Rolle deployed - vault_vpn_client_conf ist deshalb
   der volle Datei-Inhalt, hier nur 1:1 durchgereicht. #}
{{ vault_vpn_client_conf }}
